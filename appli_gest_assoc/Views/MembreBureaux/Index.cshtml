@model IEnumerable<appli_gest_assoc.Models.MembreBureau>

@{
    ViewData["Title"] = "Index";
}

<div class="container">

    <!-- Page-Title -->
    <div class="row">
        <div class="col-sm-12">
            <h4 class="pull-left page-title">Association</h4>
            <ol class="breadcrumb pull-right">
                <li><a href="#">Moltran</a></li>
                <li><a href="#">Association</a></li>
                <li class="active">Liste des associations</li>
            </ol>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <div class="panel-heading">
                        <h3 class="panel-title">Liste des associations</h3>
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-md-12 col-sm-12 col-xs-12">
                                <table id="datatable" class="table table-striped table-bordered">
                                    <thead>
                                        <tr>

                                            <th>
                                                @Html.DisplayNameFor(model => model.Membre)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.DateCreation)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Poste)
                                            </th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>


                                    <tbody>

                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Membre.PrenomMembre)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.DateCreation)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Poste)
                                                </td>


                                                <td>
                                                    <a asp-action="Edit" asp-route-id="@item.IdMembreBureau">Modifier</a> |
                                                    <a asp-action="Delete" asp-route-id="@item.IdMembreBureau">Supprimer</a>
                                                </td>
                                            </tr>
                                        }

                                    </tbody>
                                </table>

                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div> <!-- End Row -->


    </div>
</div>